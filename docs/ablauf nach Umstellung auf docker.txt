Terminal 1:
	docker compose -f docker-compose.yml -f docker-compose.dev.yml up
Terminal 2
	cd frontend
	npm run dev

Das ist praktisch dein alter Workflow — nur dass:
	node server.js
	durch Docker ersetzt wurde.

Was du wahrscheinlich sehen wirst
Frontend: http://localhost:5173
Backend API: http://localhost:3001/api

Produktion testen:
Wenn du später alles komplett testen willst: docker compose up --build


